# User Alt-Vim keys without prefix keys to switch panes.
bind -n M-Left  select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up    select-pane -U
bind -n M-Down  select-pane -D

bind -n C-S-Up resize-pane -U 10
bind -n C-S-Down resize-pane -D 10
bind -n C-S-Left resize-pane -L 10
bind -n C-S-Right resize-pane -R 10

# THEME
#set -g status-bg black
#set -g status-fg white
#set -g window-status-current-bg white
#set -g window-status-current-fg black
#set -g window-status-current-attr bold
#set -g status-interval 60
#set -g status-left-length 30
#set -g status-left '#[fg=green](#S) #(whoami)'
#set -g status-right '#[fg=yellow]#(cut -d " " -f 1-3 /proc/loadavg)#[default] #[fg=white]%d-%b-%Y  %H:%M#[default]'

# [This came from](https://gist.github.com/spicycode/1229612)
# There are a lot more settings there.
# JS.
# Color and style settings

source ~/.tmux/theme.sh
