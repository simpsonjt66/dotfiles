#!/bin/bash

# Extracts the current font name from the waybar style.css file.
# This is useful for scripts that need to know the current theme's font.

grep -oP 'font-family:\s*["'\'']?\K[^;"'\'']+' ~/.local/state/current/font/waybar.css | head -n1
